<Page x:Class="Variedades.Views.PageEstadisticas"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageEstadisticas">

    <ScrollViewer>
        <Grid>
            <!--Main Layout-->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Name="Primero" Width="*"/>
                <ColumnDefinition Name="Segundo" Width="*"/>

            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>



            <lvc:CartesianChart Grid.Row="0" Grid.Column="0" Series="{Binding seriesCollection}" />

            <lvc:CartesianChart Grid.Row="0" Grid.Column="1"  />

        </Grid>
    </ScrollViewer>

</Page>
